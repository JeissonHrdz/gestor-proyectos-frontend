import { Routes } from '@angular/router';
import { MenuLeftComponent } from './Components/menu-left/menu-left.component';

export const routes: Routes = [
    {path:'inicio',component:MenuLeftComponent}
];
